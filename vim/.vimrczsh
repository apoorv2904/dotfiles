" --- GENERAL SETTINGS ---
" Enable filetype detection, plugins, and indentation
filetype plugin indent on

" Enable syntax highlighting
syntax enable

" Set the map leader to Space (easier than default backslash)
let mapleader = " "

" --- MACOS SPECIFIC ---
" Make Vim use the macOS system clipboard (Cmd+C / Cmd+V compatibility)
" This allows you to yank (y) in vim and Cmd+V in browser, and vice versa.
set clipboard=unnamed

" Enable mouse support (allows scrolling with trackpad/mouse wheel)
set mouse=a

" Fix backspace behavior (sometimes broken on default Mac vim)
set backspace=indent,eol,start

" --- INDENTATION (Your settings) ---
set tabstop=4       " show existing tab with 4 space width
set shiftwidth=4    " indent '>' to 4 space
set expandtab       " on pressing tab insert 4 spaces
set autoindent      " maintain indent of current line on new line
set smartindent     " smarter indentation for C-like languages

" --- MAPPINGS ---
" Escape mapping
imap jk <Esc>

" Toggle Search Highlight
" Changed to <Leader>h (Space + h) because F-keys are hard to reach on Mac
nnoremap <silent> <leader>h :set hlsearch!<CR>

" Toggle Line Numbers
" Changed to <Leader>n (Space + n)
nnoremap <silent> <leader>n :set number!<CR>

" --- UI & SEARCH SUGGESTIONS ---
set number          " Show line numbers by default
set cursorline      " Highlight the current line (helps find cursor)
set showmatch       " Highlight matching parenthesis/brackets

" --- SEARCH ---
set incsearch       " Search as you type
set ignorecase      " Ignore case...
set smartcase       " ...unless capital letter used


" Remove swap files (optional, keeps directory clean)
set noswapfile

" Enable 24-bit true color (Crucial for the deep blue background)
if (has("termguicolors"))
 set termguicolors
endif

" Try to load dusk, fallback if not found on server
try
    colorscheme dusk
catch
    " If dusk isn't installed on remote, use default
    colorscheme default
endtry

