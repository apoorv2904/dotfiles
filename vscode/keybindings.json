[
    // Cmd+j = Focus Terminal (Go Down)
    {
        "key": "cmd+j",
        "command": "workbench.action.terminal.focus",
        "when": "editorTextFocus"
    },
    // Cmd+k = Focus Code (Go Up)
    {
        "key": "cmd+k",
        "command": "workbench.action.focusActiveEditorGroup",
        "when": "terminalFocus"
    }
]
